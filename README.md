# ILM Repository Defaults

This repository contains the default configuration for ILM repositories. These files are used to provide a consistent experience across all ILM repositories.

When different configuration is needed, it should be overridden in the repository itself.
